# 论文初稿 — 需要你自己完成的事项

对比模板论文后，以下内容**必须由你手动完成**，因为它们涉及个人信息、实际运行截图或需要在 Word 中操作的排版功能。

---

## 一、封面信息替换（优先级：高）

打开 `论文初稿.docx`，将封面页中的以下占位文字替换为你的真实信息：

| 占位文字 | 替换为 |
|---|---|
| 【请填写学院名称】 | 你的学院，如"计算机与软件学院" |
| 【请填写专业名称】 | 你的专业，如"软件工程" |
| 【请填写年级】 | 你的年级，如"2021 级" |
| 【请填写姓名】 | 你的姓名 |
| 【请填写学号】 | 你的学号 |
| 【请填写指导教师】 | 指导教师姓名 + 职称 |

英文封面同理（第二页）。

---

## 二、在 Word 中生成目录（优先级：高）

论文已预留了"目 录"标题页，但自动目录需要在 Word 中手动生成：

1. 打开 Word → 定位到"目 录"标题下方的空白段落
2. 菜单栏 → **引用** → **目录** → 选择一个样式插入
3. 如果目录无法自动识别标题，需要手动将各章节标题设为 Word 内置的"标题1""标题2""标题3"样式后再生成

> 提示：由于 python-docx 生成的标题使用自定义格式而非 Word 内置标题样式，你可能需要对每个章节标题重新应用 Word 样式。

---

## 三、插入系统截图（优先级：高）

论文中已用 **`[此处插入图 X.X ……]`** 标记了所有需要插入图片的位置。你需要：

### 第 3 章（系统设计）
- **图 3-1 系统总体架构图**：画一个 B/S 三层架构图（表现层 → 业务逻辑层 → 数据层），可用 draw.io / Visio / PPT 绘制
- **图 3-2 技术架构图**：展示 Flask + PyTorch + PyG + RDKit 的技术栈关系

### 第 4 章（系统实现）
- **图 4-1 数据预处理流程图**：SMILES → RDKit 解析 → 原子映射 → 边构建 → 性质计算 → PyG Data
- **图 4-2 MoleculeVAE 模型结构图**：编码器(Embedding→GCN×2→Pool→fc_mu/fc_logvar) → z → 解码器 + 预测器
- **图 4-3 训练流程图**：数据加载 → 前向传播 → 损失计算 → 反向传播 → 早停判断
- **图 4-4 分子解码流程图**：logits → 温度采样 → 价态约束 → 连通性修补 → RDKit 校验
- **图 4-5 性质导向生成流程图**：随机采样z池 → 范数裁剪 → 属性预测打分 → top-k → 解码 → 筛选 → 去重
- **图 4-6 骨架优化流程图**：Hit编码 → 多尺度微扰 → 解码 → 骨架过滤 + 规则侧链替换 → 合并去重
- **图 4-7 ~ 4-9 前端界面截图**：启动系统后分别截取三个功能面板的界面截图

### 第 5 章（系统测试）
- **图 5-1 ~ 5-3 训练模块测试截图**：训练启动、训练日志、训练完成的界面截图
- **图 5-4 ~ 5-6 生成模块测试截图**：参数设置、生成过程、生成结果的界面截图
- **图 5-7 ~ 5-8 骨架优化测试截图**：Hit 输入、衍生物结果的界面截图
- **图 5-9 训练损失曲线图**：可以用 matplotlib 画训练过程中 loss 的变化曲线
- **图 5-10 生成分子 QED 分布图**：可选，用直方图展示生成分子的 QED 分布

> 截图方法：运行 `python app.py`，在浏览器打开 `http://localhost:5000`，使用各功能后截图。

---

## 四、补充实际测试数据（优先级：中）

第 5 章的测试数据目前是估算范围值，建议你实际运行系统后填入真实数据：

1. **运行一次完整训练**（zinc_10k 数据集，50轮），记录：
   - 训练耗时
   - 最终训练/验证损失
   - 最佳模型在第几轮

2. **运行一次分子生成**（默认约束，100轮），记录：
   - 解码成功数
   - RDKit 校验通过数
   - 约束满足数
   - 最终保留分子数
   - 最佳分子的 QED、LogP 等指标

3. **运行一次骨架优化**，记录：
   - 潜在空间微扰生成数
   - 规则侧链替换生成数
   - 合并去重后总数

将以上数据替换论文第 5.4 节中的"约 XX%-XX%"为具体数值。

---

## 五、参考文献核对与补充（优先级：中）

当前参考文献共 20 篇 + 占位标记。你需要：

1. 检查正文中是否有需要标注引用的地方（如"Kipf 和 Welling 于 2017 年提出"→ 加上 [2]）
2. 核对每篇参考文献的格式是否符合学校要求（期刊/会议/年份/页码）
3. 如有需要，补充更多相关文献（建议总数 25-30 篇）
4. 确认参考文献编号与正文引用标注一一对应

---

## 六、最终排版微调（优先级：低）

在 Word 中进行最终排版调整：

- [ ] 页眉页脚：确认奇数页页眉显示论文题目、偶数页显示学校名称（已通过脚本设置，但可能需要微调）
- [ ] 页码：确认从正文第一页开始编号（摘要和目录用罗马数字或不编号，视学校要求）
- [ ] 图表编号：插入图片后，在图下方添加"图 X-X 图题"说明文字
- [ ] 表格编号：确认每个表格上方有"表 X-X 表题"
- [ ] 全文通读：检查语句通顺性、错别字、专业术语一致性
- [ ] 查重：提交前用学校指定的查重系统进行论文查重

---

## 快速检查清单

- [ ] 封面个人信息已替换
- [ ] 英文封面个人信息已替换
- [ ] 目录已在 Word 中生成
- [ ] 第 3 章的 2 张架构图已插入
- [ ] 第 4 章的 6+ 张流程图/结构图已插入
- [ ] 第 4 章的 3 张前端截图已插入
- [ ] 第 5 章的 8+ 张测试截图已插入
- [ ] 第 5.4 节的测试数据已替换为实际值
- [ ] 参考文献已核对并补充
- [ ] 正文引用标注已添加
- [ ] 页眉页脚格式正确
- [ ] 全文已通读检查
