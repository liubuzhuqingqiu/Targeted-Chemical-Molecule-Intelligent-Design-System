# 靶向化学分子智能设计系统 — 技术文档

## 一、系统概述

本系统基于**图变分自编码器（Graph VAE）**，实现从 SMILES 分子数据集训练生成模型，并提供三大核心功能：

1. **模型训练** — 上传 SMILES 数据集，训练分子图 VAE 模型
2. **性质导向分子生成** — 在潜在空间中采样，结合属性预测器引导生成符合约束的分子
3. **骨架跃迁与优化** — 将苗头化合物编码到潜在空间，通过微扰潜在向量生成同骨架衍生物

---

## 二、技术架构

```
前端 (HTML/CSS/JS + Bootstrap)
        │
        ▼
Flask Web 服务 (app.py)
        │
   ┌────┼────────────┐
   ▼    ▼            ▼
训练模块  生成模块  骨架优化模块
(train.py) (app.py)  (app.py)
   │    │            │
   ▼    ▼            ▼
┌──────────────────────────┐
│  MoleculeVAE (model.py)  │
│  编码器 / 解码器 / 预测器 │
└──────────────────────────┘
   │              │
   ▼              ▼
数据预处理      分子解码与评估
(molecule_      (reconstruct.py)
processor.py)
   │
   ▼
全局配置 (config.py)
```

---

## 三、模块详解

### 3.1 config.py — 全局配置

| 配置项 | 技术内容 |
|--------|----------|
| 原子映射 | 6 种原子类型（C/N/O/F/S/Cl）的双向索引映射、化学价态限制 |
| 模型参数 | 潜在维度 32、隐藏维度 64、10 维属性向量（QED/LogP/MW/HBD/HBA 等） |
| 约束阈值 | 分子量范围、LogP 范围、Lipinski 五规则阈值等 |
| 设备检测 | PyTorch CUDA/CPU 自动选择 |

**涉及技术：** PyTorch、Python os 模块

---

### 3.2 model.py — 分子图变分自编码器 (MoleculeVAE)

核心模型，包含三个子网络：

#### 编码器（Encoder）
- **nn.Embedding** — 将离散原子类型索引映射为连续向量
- **GCNConv × 2** — 两层图卷积网络（PyTorch Geometric），聚合邻居节点信息学习分子结构特征
- **global_mean_pool** — 图级别平均池化，将变长节点特征压缩为固定维度的图向量
- **fc_mu / fc_logvar** — 全连接层，输出潜在空间的均值 μ 和对数方差 log σ²

#### 解码器（Decoder）
- **decoder_atoms** — MLP（Linear(z,128)→ReLU→Linear(128, max_nodes×6)），从潜在向量 z 预测每个节点位置的原子类型（6 类）
- **decoder_edges** — MLP（Linear(z,128)→ReLU→Linear(128, max_nodes²×5)），从 z 预测节点对之间的键类型（5 类：无键/单键/双键/三键/芳香键）

#### 属性预测器（Predictor）
- **predictor** — MLP（Linear(z,64)→ReLU→Linear(64,10)），从 z 预测 10 维分子性质
- 用途：在生成阶段引导采样方向，使生成分子趋向目标性质（高 QED / 特定 LogP 等）

#### 关键算法
- **重参数化技巧（Reparameterization Trick）** — z = μ + ε·σ，使随机采样过程可反向传播梯度

**涉及技术：** PyTorch、PyTorch Geometric（GCNConv、global_mean_pool）、VAE 理论

---

### 3.3 molecule_processor.py — 数据预处理

| 函数 | 功能 | 技术 |
|------|------|------|
| `calculate_sa_score` | 简化版合成可达性评分 | RDKit 环信息、杂原子统计 |
| `smiles_to_graph` | SMILES → PyG 图数据（Data 对象） | RDKit 分子解析、PyTorch Geometric Data |

#### smiles_to_graph 流程
1. RDKit 解析 SMILES 为分子对象
2. 过滤：仅允许 C/N/O/F/S/Cl 六种原子，最多 50 个原子
3. 节点特征：原子类型索引（通过 ATOM_TO_IDX 映射）
4. 边构建：遍历化学键，构造双向边索引 + 键类型属性（单键=1, 双键=2, 三键=3, 芳香键=4）
5. 标签计算：RDKit 计算 10 维分子性质作为训练标签

**涉及技术：** RDKit（Chem、QED、Descriptors、rdMolDescriptors）、PyTorch Geometric Data

---

### 3.4 train.py — 模型训练

#### 损失函数 vae_loss
总损失 = 原子重构 + 3×键重构 + β×(KL散度/batch_size) + 0.3×属性预测

| 损失项 | 计算方式 | 作用 |
|--------|----------|------|
| 原子重构损失 | 交叉熵（CrossEntropy），忽略填充位 | 衡量原子类型还原准确度 |
| 键重构损失 | 交叉熵 + 掩码（仅计算有效节点对） | 衡量化学键还原准确度 |
| KL 散度 | -0.5 × Σ(1 + log σ² - μ² - σ²)，除以 batch_size 做 per-sample 归一化 | 约束潜在空间接近标准正态分布 |
| 属性预测损失 | 均方误差（MSE） | 让潜在空间编码分子性质信息 |

#### 训练策略
| 策略 | 实现 |
|------|------|
| KL 退火（Warmup） | 前 30% 轮次 β 从 0 线性增长到 0.05，防止 KL 坍缩 |
| 早停（Early Stopping） | 验证损失连续 N 轮不改善则终止（默认 patience=10，可配置） |
| 学习率调度 | ReduceLROnPlateau（patience=5），验证损失停滞时自动减半 |
| 梯度裁剪 | clip_grad_norm_，max_norm=1.0，防止梯度爆炸 |
| 数据集分割 | random_split，默认 90% 训练 / 10% 验证 |

**涉及技术：** PyTorch（Adam、ReduceLROnPlateau、clip_grad_norm_）、PyTorch Geometric（DataLoader、to_dense_batch、to_dense_adj）

---

### 3.5 reconstruct.py — 分子解码与评估

#### 3.5.1 解码器 logits_to_smiles

将模型输出的原子/键 logits 重建为有效 SMILES：

1. **原子采样** — `torch.multinomial` 概率采样（温度 0.8），避免 argmax 导致的模式坍缩
2. **键采样** — `torch.multinomial` 概率采样（温度 0.6）
3. **化学价态约束** — 根据 ATOM_VALENCY_LIMIT 限制每个原子的成键数
4. **键类型优先级** — 按 单键→芳香键→双键→三键 顺序添加
5. **连通性修补** — 若图不连通，补充单键连接
6. **RDKit 校验** — SanitizeMol + 取最大连通片段

#### 3.5.2 分子评估 evaluate_molecule

| 评估项 | 技术 |
|--------|------|
| QED（药物相似度） | RDKit QED.qed() |
| LogP（脂水分配系数） | RDKit Descriptors.MolLogP() |
| 分子量 / HBD / HBA / TPSA | RDKit Descriptors 系列函数 |
| 可旋转键数 | RDKit rdMolDescriptors.CalcNumRotatableBonds() |
| SA Score（合成可达性） | 自定义简化公式 |
| Lipinski 五规则 | MW≤500, LogP≤5, HBD≤5, HBA≤10 |
| Veber 规则 | RotBonds≤10, TPSA≤140 |

#### 3.5.3 ADMET 预测（内嵌）

| 指标 | 方法 |
|------|------|
| 水溶性 log(S) | ESOL 经验公式（Delaney, 2004） |
| 渗透性 | 基于 TPSA 与 LogP 的经验规则 |
| 血脑屏障透过性 | 基于 TPSA、LogP、MW 的经验规则 |
| 毒性警示子结构 | SMARTS 模式匹配（硝基、肼/偶氮等） |

#### 3.5.4 骨架工具

| 函数 | 技术 |
|------|------|
| `get_murcko_scaffold_smiles` | RDKit MurckoScaffold 提取 Bemis-Murcko 骨架 |
| `scaffold_tanimoto_similarity` | Morgan 指纹（ECFP4）+ Tanimoto 相似度计算 |

**涉及技术：** RDKit（Chem、QED、Descriptors、MurckoScaffold、Morgan Fingerprint、SMARTS）、PyTorch

---

### 3.6 app.py — Flask Web 服务

#### 三大核心功能

##### 功能一：模型训练（/start_train）
- 前端上传 SMILES 数据集 + 设置超参数
- 后端 threading.Thread 异步训练
- 前端轮询 /get_train_status 获取进度

##### 功能二：性质导向分子生成（/generate）
流程：
1. **性质引导采样** — `select_guided_latents`：先随机采 z 池并裁剪范数至 [0.5, 3.0]，用属性预测头打分，选 top-k z
2. **解码** — 调用 `model.decoder_atoms/decoder_edges` + `logits_to_smiles`
3. **RDKit 校验 + 约束筛选** — MW/LogP/QED/SA 等多维约束
4. **Tanimoto 去重** — Morgan 指纹（ECFP4）相似度去重，保留多样性
5. **评分排序** — score = 0.3×QED + 0.2×LogP − 0.2×violations（仅当 Lipinski 违规数 > 1 时触发惩罚）

##### 功能三：骨架跃迁与优化（/scaffold_optimize）
两阶段流程：
- **阶段一（潜在空间微扰）** — Hit 编码→z_hit 附近多尺度微扰→属性预测筛选→解码→骨架过滤
- **阶段二（规则侧链替换）** — RDKit RWMol 直接在末端原子上做原子类型替换或碳扩展
- 合并去重后按目标性质排序

#### 其他技术
| 功能 | 技术 |
|------|------|
| 分子 2D 图像渲染 | RDKit Draw.MolToImage → Base64 编码 |
| 异步任务 | Python threading.Thread |
| 状态轮询 | 前端 setInterval + /get_*_status API |

**涉及技术：** Flask、threading、RDKit、PyTorch、PyTorch Geometric

---

### 3.7 前端 — templates/index.html + static/js/main.js

| 模块 | 技术 |
|------|------|
| UI 框架 | Bootstrap 5 + Bootstrap Icons |
| 交互逻辑 | 原生 JavaScript ES6+ Class |
| 分子展示 | Base64 PNG 图片（后端 RDKit 渲染） |
| 状态轮询 | Fetch API + setInterval 轮询后端状态接口 |
| 衍生物浏览 | 前后切换器，Hit 与衍生物对比表格 |

---

## 四、技术栈总结

| 层级 | 技术 |
|------|------|
| 深度学习框架 | PyTorch |
| 图神经网络 | PyTorch Geometric（GCNConv、global_mean_pool、DataLoader） |
| 生成模型 | 变分自编码器 VAE（重参数化、KL 散度、KL 退火） |
| 化学信息学 | RDKit（SMILES 解析、分子性质计算、骨架提取、指纹相似度、ADMET、2D 渲染） |
| Web 后端 | Flask（REST API、多线程异步任务） |
| Web 前端 | HTML5 + CSS3 + JavaScript + Bootstrap 5 |
| 数据格式 | SMILES（.smi 文件）、PyTorch 模型文件（.pth） |

---

## 五、文件结构

```
项目根目录/
├── config.py              # 全局配置（原子映射、模型参数、约束阈值）
├── model.py               # MoleculeVAE 模型定义（编码器/解码器/预测器）
├── molecule_processor.py  # 数据预处理（SMILES→图、SA Score）
├── train.py               # 训练逻辑（损失函数、训练循环、早停）
├── reconstruct.py         # 解码+评估（logits→SMILES、ADMET、骨架工具）
├── app.py                 # Flask Web 服务（生成、骨架优化、训练接口）
├── templates/
│   └── index.html         # 前端页面
├── static/
│   ├── css/               # 样式文件
│   └── js/
│       └── main.js        # 前端交互逻辑
├── models/                # 训练好的模型文件 (.pth)
├── datasets/              # SMILES 数据集 (.smi)
└── requirements.txt       # Python 依赖
```

---

## 六、核心算法流程

### 6.1 训练流程
```
SMILES 数据集 → smiles_to_graph() → PyG DataLoader
     → MoleculeVAE.forward()
         → encode(GCN) → μ, log σ²
         → reparameterize → z
         → decode → atom_logits, edge_logits
         → predict_properties → properties
     → vae_loss(重构+KL+属性)
     → Adam 优化 + 早停 + 学习率调度
     → 保存最佳模型 .pth
```

### 6.2 性质导向生成流程
```
随机采样 z 池 → 范数裁剪[0.5,3.0] → 属性预测头打分 → 选 top-k z
     → decode → logits_to_smiles(概率采样)
     → RDKit 校验 + 约束筛选
     → Tanimoto 去重 → 评分排序 → 返回前端展示
```

### 6.3 骨架跃迁与优化流程
```
Hit SMILES → model.encode() → z_hit
     → z_hit + 多尺度噪声 → 候选 z
     → 属性预测筛选 top-k
     → decode → logits_to_smiles
     → Murcko 骨架过滤(保留同骨架)
     ↓
规则侧链替换(补充)
     → RWMol 末端原子替换 / 碳扩展
     ↓
合并去重 → 按目标性质排序 → 展示
```
